<!DOCTYPE html>
<html>
  <head>
    <title>Msgr</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'custom' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="page-header container">
      <h1 class="pull-left"><%=link_to "msgr", posts_path %><small> featuring a compelling sub header</small></h1>
      <% if session[:user_id] != nil %>
        <h3 class="pull-right">User: <%=User.find(session[:user_id]).name %> | <%= link_to "Log out", logout_path, method: :delete, id: "sign-out" %></h3>
      <% else %>
        <h3 class="pull-right"><%=link_to "Log in", login_path%> | <%= link_to "Sign Up", new_user_path %></h3>
      <% end %>
    </div>
    
    <div class="container" id="body">
      <div class="alert alert-success alert-dismissible hidden" id="sign-out-alert" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="close"><span aria-hidden="true">&times;</span></button>
        You have been signed out.
    </div>
      <%= yield %>
    </div>
    <% console %>
  </body>
</html>
